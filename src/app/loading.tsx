import { Spinner } from '@/components/loading-elements/index';

// The animated loading component that is displayed when the application is loading a page.
export default function Loading() {
  return <Spinner />;
}

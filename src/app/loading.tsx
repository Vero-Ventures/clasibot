import { Spinner } from '@/components/loading/index';

// The animated loading component that is displayed when the application is loading.
export default function Loading() {
  return <Spinner />;
}
